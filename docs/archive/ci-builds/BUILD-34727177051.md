# CI build 34727177051 — linked intermediate (NOT a compatibility release)

## Provenance
- Commit: `6dd10ac714e8f73bc5eac9340c2b3ff3a543cf45` (`launch-pipeline`)
- Branch pushed: `launch-pipeline` → `KakarottoCake/usbloadergx-riivolution`
- Run: https://github.com/KakarottoCake/usbloadergx-riivolution/actions/runs/34727177051
- Workflow: `Build binaries` (`Build binaries`), conclusion `success`
- Toolchain: `devkitpro/devkitppc:20250527`, `make release -j2` (54 s compile)
- Upload steps (`Package`, `Upload binary`, `Upload debug binary`) all
  `success` — artifact upload DOES run for this branch (`ALLOW_UPLOADS`
  is true here; only the `diag/reloc-evidence`-scoped steps skipped).

## Artifacts (downloaded 2026-09-13, verified below)
| file | bytes | SHA-256 |
| --- | --- | --- |
| `usbloadergx_6dd10ac/apps/usbloader_gx/boot.dol` | 5198464 | `625E98257CB033CE6DFB8F0CC9770080062BB63B8ABA362481D92B18AB15658C` |
| `usbloadergx_6dd10ac_debug/boot.elf` | 33280684 | `04D8360F6ECFD04A00B139CC23E039CFE88859EC083EB83FE2002AEF90A99CB5` |
| `usbloadergx_6dd10ac_debug/build/boot.elf.map` | 5215332 | `B1D3220E9434BF6778C2758E2572AD9452DA57715214959D41CD523424B88B15` |

(`icon.png`/`meta.xml` ride along in the package; hashes above cover the
linked outputs. Binaries themselves live on the CI run, not in git.)

## Code-presence checks (ASCII scan of `boot.dol`, all present)
`boot view disarmed`, `early placement applied`, `router divergence`,
`stat/compose disagreement`, `manifest RIV1`, `remapped by name`,
`executable patch routed`, `DOL verified`.
(`ResolveLateOffsets`/`earlyKey` are code/comment-only; absence from string
table is expected, not a mismatch.)

## Host verification at the same commit (local, `sh hosttests/run.sh`)
All suites passed, including `test_patchplan` (106), `test_bootview` (67),
`test_planparity` (112). Changed TUs also clean under local
`powerpc-eabi-gcc -fsyntax-only` (wii MACHDEP); the local full link is not
attempted (pinned toolchain only; local 16.1.0 has the known unrelated
portlibs/libogc socket skew).

## What this build proves and does not prove
- Proves: the integrated tree (plan → boot view → apploader wiring →
  manifest, SMG2 retired, in-place-only placement) compiles and links on
  the pinned toolchain with no new warnings-as-errors; the exact bytes
  above are reproducible from the recorded commit via CI.
- Does NOT prove: any mod boots or plays (no hardware run under this
  order); grown tables (still refused by policy); partial file serving
  post-boot (still refused); DVD9 overlap (still refused). Outcome lines
  in boot logs mean exactly what their site comments say — see the
  `FST_STAGED` evidence note in `RiivoConfig.hpp`; absence of blink codes
  is not an installation proof.
